{
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \".venv\\Scripts\\python.exe -m uvicorn app.main:app --reload\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "prettier": "^3.9.6"
  }
}
